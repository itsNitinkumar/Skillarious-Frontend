/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'your-storage-domain.com',
      "avatar.iran.liara.run"],
  },
};

export default nextConfig;
